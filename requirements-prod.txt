# Production Requirements
# Installs the same pinned, tested base set used by CI/tests, plus the
# production WSGI server. Keeping a single source of truth (requirements.txt)
# prevents prod from running different FastAPI/Starlette versions than what
# the test suite validates.
-r requirements.txt
gunicorn==21.2.0
